#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * print_listint - A function that prints the number of elements
 * in a list
 * @h: The first element of the list
 *
 * Return: The number of nodes
 */
size_t print_listint(const listint_t *h)
{
	listint_t *head;
	listint_t *new;
	h = "";
}
