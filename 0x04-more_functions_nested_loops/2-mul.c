#include <stdio.h>
#include "main.h"

/**
 * mul - Multiply two integers
 * @a: First operand for the multiplication
 * @b: Second operand for the multiplication
 * Return: a * b
 */
int mul(int a, int b)
{
	return (a * b);
}
