#include <stdio.h>

/**
 * main - A program that prints my name
 * @argc: The number of argument passed
 * @argv: The argument passed with an executable
 *
 * Return: Always 0
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
