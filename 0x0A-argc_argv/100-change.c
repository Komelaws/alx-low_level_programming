#include <stdio.h>

/**
 * main - A program that prints the minimum number of
 * coins to make change for an amount of money
 * @argc: The number of arguments passed
 * @argv: The arguments passed
 *
 * Return: 1 if argc < 1 && > 1
 */
int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
	{
	}

	return (0);
}
